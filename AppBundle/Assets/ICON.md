# Piko App Icon

`AppIcon.icon/Assets/Piko.png` is the opaque, full-bleed 1254-by-1254 source. The built-in image generation tool revised the first icon on 2026-09-06. It uses a continuous ice-blue background and a geometric uppercase P with a flush top, consistent strokes and a smooth bowl. The menu-bar template symbol is unchanged.

The source intentionally has no transparent margin, rounded-square mask or outer shadow. Icon Composer supplies the native mask. The old transparent, pre-rounded ICNS-only artwork appeared inset on a white system plate in the user's macOS 26 Dock.

`Scripts/generate-icon.swift` compiles `AppIcon.icon` with Xcode's `actool`, shipping both `Assets.car` (native icon stack and appearance variants) and `AppIcon.icns` (legacy fallback). `CFBundleIconName` and `CFBundleIconFile` both reference `AppIcon`. Do not replace this with ICNS-only packaging or bake a second rounded tile into the artwork.

Apple documents this format and automatic legacy generation in [Creating your app icon using Icon Composer](https://developer.apple.com/documentation/xcode/creating-your-app-icon-using-icon-composer).

## Revision Prompt

Use case: precise-object-edit. Edit the supplied Piko app icon. Preserve the clear ice-blue to rich azure/cobalt palette and the white ceramic monogram identity, but fix BOTH the background and the letter. OUTPUT: one square 1024x1024 full-bleed OPAQUE app-icon artwork, not a rounded app icon mockup. Fill every pixel right through all four edges and corners with a refined smooth blue gradient; absolutely NO transparent padding, rounded tile boundary, outside shadow, white/gray plate, border, rim, frame, or icon within an icon. The operating system will supply the rounded mask, so the artwork itself MUST be a solid full-bleed square. Simplify the background to smooth ice blue upper-left and azure lower-right, with at most one broad very subtle tonal sweep, not repeated ribbon stripes. Rebuild the central white P as a precise, optically balanced, confidently geometric uppercase P monogram: one broad straight vertical stem, one clean rounded rectangular upper bowl, an ample rounded-square blue counter, constant stroke thickness, softly rounded exterior corners. Top of stem and bowl are FLUSH and form one clean continuous top edge, no stem sticking above bowl. The bowl's bottom is smooth and level, no notch, stair-step, protrusion, diagonal hook, or malformed shoulder. Stem bottom softly squared rather than capsule shaped. Monogram overall height about 60% of canvas, width about 43%, centered optically, preserve generous breathing space within the blue square. Restrained ceramic dimension with near-white face and a VERY subtle cool-blue shallow side shadow, NOT thick extrusion or pillowy bubbly inflated plastic. Clean precision edges readable at 32px. One letter only, no wordmark, no text captions, no small symbols, no outer frame, no medical heartbeat, no checkerboard. This is finished artwork for native icon packaging, not an illustration of an app icon.
